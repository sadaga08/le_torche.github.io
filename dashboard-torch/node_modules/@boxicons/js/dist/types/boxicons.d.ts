import type { IconDefinition, IconOptions, CreateIconsOptions } from './types.js';
/**
 * Create an SVG string from an icon definition
 */
export declare function createSvgString(icon: IconDefinition, options?: IconOptions): string;
/**
 * Minimal SVG element interface for SSR compatibility
 */
interface SVGElementLike {
    outerHTML: string;
    setAttribute(name: string, value: string): void;
    getAttribute(name: string): string | null;
}
/**
 * Create an SVG element from an icon definition (similar to Lucide's createElement)
 * In browser environments, returns an actual SVGSVGElement.
 * In Node.js/SSR, returns an object with outerHTML for string rendering.
 */
export declare function createElement(icon: IconDefinition, options?: IconOptions): SVGSVGElement | SVGElementLike;
/**
 * Replace icons by scanning the DOM for elements with the data-bx attribute
 * and replacing them with SVG icons. Similar to Lucide's createIcons function.
 *
 * @example
 * ```js
 * import { getIcons, Menu, ArrowRight, Globe } from '@boxicons/js';
 *
 * getIcons({
 *   icons: {
 *     Menu,
 *     ArrowRight,
 *     Globe
 *   }
 * });
 * ```
 */
export declare function getIcons(options: CreateIconsOptions): void;
export { createElement as createSvgElement };
export { getIcons as createIcons };
//# sourceMappingURL=boxicons.d.ts.map